# @vinkugor/nacos-mcp-win32-arm64

Platform binary for [@vinkugor/nacos-mcp](https://www.npmjs.com/package/@vinkugor/nacos-mcp).

**Do not install directly.** Install `@vinkugor/nacos-mcp` instead — npm will automatically select the matching platform binary.
